[Service]
# TR-160: route the scheduler's Hermes calls through the task-router proxy so the router
# chooses the model per prompt (the whole point of the proxy work). Verified first by
# scripts/proxy_acceptance.py: 11/11 gates green, including the scheduler's exact
# POST /v1/responses streaming shape, the 400s-silent-turn allowance, and the failure path.
# Rollback: delete this file (or uncomment the line below) and restart the service.
# Kept in the repo as scripts/systemd/coding-hermes-scheduler-gateway-via-router.conf
Environment=SCHEDULER_GATEWAY_URL=http://127.0.0.1:9391
