# TR-037 closeout — weekly research lane 3/3 (rankings + misc), 2026-09-11

Executed 2026-09-11 ~19:14–20:10 UTC by the task-router foreman (tick
task-router-2026-09-11-19-14-35). Data commit: c819f2b (temporary_discounts
sweep + chains snapshot). This document is the evidence artifact for every
criterion clause. All commands run from the repo root in
`~/.hermes/venvs/board/bin/python3`.

## 1. Benchmark freshness — checked, no ranking shifts to act on

- `data/tables/benchmarks.jsonl`: freshest evidence rows are TODAY-dated —
  `research-bench-2026-09-11` (SWE-bench Verified 79.0 vendor/zenmux roundup;
  morphllm consensus AA Intelligence 35 vs Pro-0813 36, within 1pt) and
  `research-bench-2026-09-10` (4 rows: webscraft migration analysis,
  r/codex multi-model bench, coderabbit tier guide, Sol promo-terms note).
  The 122× battery-T1/T2/T4 + 61× T5 floor rows are the deterministic
  in-house battery, refreshed by seed, not a freshness concern.
- `model_perf.jsonl` / `model_tier.jsonl` regenerated by today's seed runs
  (file mtimes 2026-09-11 13:27, lane-1 tick earlier today). No new benchmark
  number found that would move a `model_tier` level: the 09-10/09-11 rows
  explicitly conclude "within 1pt / not a regime shift / supports current
  standing".
- `quality_estimates.jsonl` (11 rows, Aug-29 vintage): consumed by
  `router_seed.py:_load_quality_estimates()` as static priors; no new
  quality-estimate source surfaced this week. No-change.

**Verdict: no-change verified 2026-09-11 (sources: in-repo benchmarks.jsonl
evidence rows dated 09-10/09-11; seed freshness by mtime).**

## 2. Chain-head sanity — PASS on both registered projects

`router_spawn.py <project> --format json` captured 2026-09-11T19:14 UTC,
gate=OPEN, fail-open contract intact, `fallback_used: false`:

| project | static chain #1 (docs/chains-2026-09-11.md) | live resolve head | explanation |
|---|---|---|---|
| coding-hermes-scheduler (P1_CODING) | 1. opencode-go/mimo-v2.5 $0.013 | hop 5: neuralwatt/glm-5.2-short-fast | hops 1–4 excluded by gates, see below |
| hivemind-work (P2_AGENTIC) | 1. ollama-cloud/kimi-k2.7-code $0.050 | hop 4: zai-glm/glm-5.3-flash | hops 1–3 excluded by gates, see below |

Price-view note: the live JSON reports blended PUBLIC price (neuralwatt
glm-5.2-short-fast $1.572 = 0.96×1.45 + 0.04×4.5; zai glm-5.3-flash $0.082),
while chain ranking uses NORMALIZED price ($0.047 / $0.069) — the two views
agree; ranking order matches the static chain exactly.

The live order MATCHES the static chain order — the head differences are
purely runtime gate exclusions, not price errors ("a stale price must not
leave the wrong model on top" — verified: no stale price moved anything):

- ollama-cloud lanes: health DOWN since 2026-09-10T23:00Z — HTTP 402
  fleet-wide = the documented billing gate
  (`docs/pitfall-2026-09-11-stale-probe-defaults-and-mirror-rules.md` §3:
  "the grandfathered Max $100 plan went payment-required overnight... the
  health gate blocks admission while DOWN"). The judge's earlier observation
  of "ollama-cloud health DOWN" exclusions CONFIRMS this record; it does not
  contradict it.
- opencode-go / opencode-go-2: health DOWN (19:00:04Z probe) + quota GATED
  (account 2).
- clinepass: weekly cap EXHAUSTED (probe 2026-09-05, uniform reset ~02:05 UTC
  — weekly cadence, self-heals).
- fireworks-ai / commandcode: quota GATED: blocked (no keys yet — deliberate
  fail-closed per 09-11 registry commit 1d74609).

## 3. Discount window sweep — 3 expired rows dropped, none expiring

`data/tables/temporary_discounts.jsonl` before: 22 rows → after: 19 rows
(commit c819f2b). Dropped the 3 rows with `valid_to: 2026-09-09` (expired 2
days ago): `minimax-m2.7:free`, `minimax-m3:free`, `glm-5.2:free` (all
clinepass). Safety: they were already doubly inert — `pricing/helpers.py
active_discounts()` excludes expired windows at pricing time, and all three
models.jsonl rows carry `disabled: true, "UPSTREAM REMOVED 2026-09-09"`.

Remaining 19 rows: all `valid_to: null` (open-ended) — **no rows expiring in
the next 30 days**. One live-window row added this week (09-11):
`ling-3.0-flash-vl:free` (id-form note: served under org-prefixed form per
GET /api/v1/models; registry keeps bare-id convention).

## 4. Evidence freshness (or-spot rows >30d) — none require refresh

`models.jsonl` or-spot evidence census: 5 rows @ `or-spot-2026-08-27`
(15 days old) + 2 rows @ `or-spot-2026-09-11` (today). Threshold is ~30 days
→ **no refresh due**. Static official-formula rows (zai-glm, evidence
"official formula"): re-verified TODAY against docs.z.ai by lane 2 (tick 35,
commit 1d74609 series; plan_terms note "OFFICIAL multipliers CHANGED
(docs.z.ai/devpack/overview + teamplan)" — already applied this morning).
Price-evidence date census across models.jsonl: 94× 09-10, 76× 09-11,
18× 08-27 (the or-spot rows above), rest newer than 30d except 3× 08-28
(disabled catalog-artifact rows, inert).

**Verdict: no-change verified 2026-09-11 (sources: models.jsonl price_evidence
census; plan_terms zai-glm note re-verified 2026-09-11).**

## 5. fallback_lanes sane — verified against current prices/state

`data/tables/fallback_lanes.jsonl` unchanged (3 rows): deepseek-foreman
(lanes 1–2, PAYG cron-key) + deepseek-duckbrain-sync (lane 3). Checked live:
lane 3 (`deepseek-duckbrain-sync/deepseek-flash`) is currently quota GATED
("blocked") in the 19:14 resolve — temporary (PAYG balance), self-heals on
top-up; lanes 1–2 not gate-blocked. Prices are subscription flat rates, not
affected by this week's repricing. No stale mapping: the rename fallout
(TR-038) did not touch fallback lane model ids for lanes 1–2; lane 3's
`deepseek-flash` id still resolves server-side (appears in live chains as a
valid hop, gated only by quota).

## 6. Findings filed

- **TR-039 filed on the board** (row appended this tick, see events.jsonl):
  "commandcode + fireworks-ai lanes lack model_tier derivation — 200
  ROUTER-MISS lines per router_spawn resolve (commandcode 65, aws-bedrock 50,
  fireworks-ai 14)". Not a regression (fail-closed by design for no-key
  providers), but the noise floor for every embedded consumer and a ranking
  gap once keys land. Follow-up row for a worker tick.
- **gitreins evaluator cap starvation (fixed here, local config)**: two
  `task complete` runs returned empty-bell "Tier 2: INCOMPLETE" with
  `items: []`; stderr showed "Eval cap exceeded: Input token budget (4.0M)
  exceeded" — this repo's 1.2MB registry.json blows the default 4M input
  budget. Raised `.gitreins/config.yaml` evaluator.max_input_tokens 4M → 12M
  (local config.yaml is tracked by gitreins install semantics; fleet repos
  with big generated tables need the same bump — candidate fleet-skill note).
- ollama-cloud 402 billing gate persists (human action: check ollama.com
  billing) — already recorded in the 09-11 pitfall doc; restated here because
  it gates 5 chain hops today.

## Verification chain

- Data: commit c819f2b (temporary_discounts 22→19, chains snapshot).
- Tests: `./scripts/gitreins-guard-tests.sh` → 287 passed (2026-09-11
  ~14:2x local), gitreins Tier 1 guards PASS at commit time.
- Closeout/board: this doc + TR-037 completion event + TR-039 row (board
  commit this tick), pushed to origin/main.
- CI: `gh run list --repo coding-hermes/task-router --limit 3` → 3/3 success
  (checked 19:4x UTC, before closeout push).
