# TR-036 — weekly research lane 2/3: provider plan-terms verification + open pricing gaps (2026-09-11)

Scope of this lane: **provider / plan data only** (billing_model, plan_cost,
included models, multipliers, quota windows, ToS/data class). Lane 1 =
catalog freshness (2026-09-10), lane 3 = rankings/misc. All claims cite
sources fetched THIS session (2026-09-11, -05). "models.dev" =
https://models.dev/api.json (fetched live in-browser 2026-09-11; diffed
against the 09-10 cache — zero drift on all sticker providers).

## Headline: all five tracked open gaps attacked — four closed, one narrowed

| # | gap (tracked in plan_terms notes) | verdict |
|---|---|---|
| a | ollama-cloud per-model $/M (JS-walled) | **CLOSED** — rates now public (ollama.com/pricing model table; library pages readable in a real browser). All 21 lane prices verified. |
| b | opencode-go per-model req-per-5h | **CLOSED** — opencode.ai/go publishes a per-model "Est. requests / 5 hr" table (27 models). Researched into plan_terms; 26 lanes re-priced by the bucket formula. |
| c | synthetic pack economics vs sticker cap | **MEASURED** — pack math documented in plan_terms; sticker-cap pricing retained as deliberately conservative. |
| d | kimi-for-coding credit refresh terms | **CLOSED** — plans re-published: Moderato $15/mo (annual), Allegretto $31, Allegro $79, Vivace $159; weekly-refreshed quotas confirmed. |
| e | clinepass features.discount=0.5 flag | **CLOSED — flag does not exist** (live GET /api/v1/plans 2026-09-11: Monthly $9.99 carries only `fee_dollars: 0.61`; Annual 7992¢ carries `fee_dollars: 2.60`). Caps confirmed: $1B/5h, $2.5B/7d, $5B/30d. |

## Per-provider verdict table

| provider | verdict | evidence (all fetched 2026-09-11) |
|---|---|---|
| clinepass | **TERMS VERIFIED + flag closed** — $9.99/mo, 2-5x usage, 13-model included set, caps 1B/2.5B/5B; reference prices unchanged; :free lanes ALL alive (19/19; listing uses org-prefixed ids — `google/gemma-4-31b-it:free` etc. — bare-id convention kept, org form noted); 7 paid non-included lanes added as documented PAYG gaps; 15 :free rows re-enabled (the 08-28 "400 not served" verdict was an id-form artifact) | docs.cline.bot/getting-started/clinepass; api.cline.bot/api/v1/plans + /models (Bearer key); models.dev `cline-pass` |
| opencode-go | **PLAN + RATES VERIFIED, re-priced** — $10/mo Go, $12/5h window; per-model req/5h table captured (K3 110 … Muse-contributor 45,300; DeepSeek V4.1 Flash 4x promo = 26,000 marked "limited time"); formula cross-check: 12/110/31250·1e6 = 3.4909 ≈ committed official row 3.49, k2.7-code 0.2844 ≈ 0.284, mimo 0.0128 ≈ 0.013 | opencode.ai/go usage table; opencode.ai/docs/zen |
| opencode-go-2 | no-change verified (mirror account; same pool) — mirror evidence rows untouched | opencode.ai/go (same plan) |
| kimi-for-coding | **PLAN RENEWED (cheaper)** — Moderato $15/mo annual ($19 monthly-equiv; legacy $19 row superseded), weekly-refreshed quotas, K3 on Moderato+ | kimi.com/code plans page |
| grok-build | **GATE STAYS ON** — grok.com/plans (JS app) shows the shared Chat/Imagine/Voice/**Build** pool ("Significantly higher usage across Chat, Imagine, Voice & Build" on Plus/Heavy) with **no published pool size**; SuperGrok $30/mo unchanged (multiple secondary sources + plan page structure) | grok.com/plans; suprmind.ai/hub/grok/pricing; felloai.com/grok-pricing |
| synthetic | **PACK ECONOMICS MEASURED** — $1/day or $30/mo, 500 req/5h per pack, 1 concurrent/model/pack, embeddings excluded from rate limit; 7 always-on models incl.; PAYG stickers moved: **GLM-5.2 $1.40→$1.00 in** (live API pricing), 5.3-flash 0.15/0.50, K3 3/15, Nemotron NVFP4 0.30/1.00. At 31,250 tok/req a $30 pack clears $60-432 sticker value → sticker-cap retained as conservative | synthetic.new/pricing; dev.synthetic.new/docs/api/models; api.synthetic.new/openai/v1/models (public JSON) |
| ollama-cloud | **GAP CLOSED + BILLING FLAGGED** — public per-model price table now exists (Base + Peak tables); all 21 lane stickers verified against it. Plan lineup now Free/$20 Pro/$100 Max/$500 Team as CREDIT buckets (Max $300 credits/mo, 10 concurrent). **402 fleet-wide since 09-10 23:00Z = billing event; human must check ollama.com billing** (no balance endpoint; fleet can't self-verify) | ollama.com/pricing; ollama.com/library/* (glm-5.3, deepseek-v4-flash, kimi-k3, gpt-oss:120b-cloud …) |
| neuralwatt | no-change verified — stickers identical (GLM-5.2 1.45/4.5, K3 3/15, flex variants, ds-v4-flash 0.14/0.28); kWh conflict still open (auth-walled portal) | models.dev `neuralwatt` (live vs 09-10 cache, zero drift) |
| minimax | no-change verified — M3 0.30/1.20, M2.7 0.30/1.20, highspeed 0.60/2.40 unchanged | models.dev `minimax`; minimax-ai.chat pricing |
| stepfun | no-change verified — step-3.7-flash 0.20/1.15 official (registry 0.208 blended within formula tolerance), 3.5-flash 0.10/0.30 | platform.stepfun.ai/docs/en/guides/pricing/details |
| groq | no-change verified — gpt-oss-120b 0.15/0.60, gpt-oss-20b 0.075/0.30, qwen3.6-27b 0.60/3.00, qwen3.8-27b 0.80/4.00; **compound/compound-mini unpriced ("Contact Sales") → NULL rows stay documented gaps** | console.groq.com/docs/models |
| zai-glm | **MULTIPLIERS CHANGED (official revision)** — plan docs now list ONLY glm-5.3 6.9/1.7/24 and glm-5.3-flash 2.3/0.56/8; glm-5.2/5.1 auto-route to 5.3 rates (unchanged); **glm-4.7 now auto-routes to GLM-5.3-FLASH rates** (was its own 4.6 multiplier); 5-turbo/4.6v multipliers gone; Max 28K/140K credits unchanged; off-peak 50% (Mon-Fri 14-18 SGT peak) unchanged. Registry 5.3-rate rows already correct; glm-4.7 row superseded (rate = flash 0.069 now — left as documented supersession, chain-neutral at -0.069 delta) | docs.z.ai/devpack/overview; docs.z.ai/devpack/teamplan.md; docs.z.ai/devpack/notice/usage-revision.md; docs.z.ai/guides/overview/pricing |
| deepseek | no-change verified — PAYG peak/off-peak tariffs unchanged (flash 0.15/0.30 in, 0.60/1.20 out; pro 0.66/1.32, 1.98/3.96; peak 01-04 + 06-10 UTC Mon-Fri); **V4 Pro API extended past 2026-09-14** (official notice); concurrency 2500/500 | api-docs.deepseek.com/quick_start/pricing |
| meta-model | no-change verified — Standard $1.25 in / $4.25 out / $0.15 cached (all 1.1/1.2/1.3); Contributor $0.10/0.20; Bane's $50 Power sub = Muse Code CLI-only, does not bill API keys (plan note stands) | ai.developer.meta.com/docs/pricing-rate-limits |
| muse-spark | no-change verified (contributor-row mirror of meta-model) | same |
| openai-codex | partial — models.dev `openai-codex` entry vanished (provider id absent from today's api.json; models live under `openai`/`azure`); stickers (gpt-6-astra 10/50, 5.6-sol 4/20, luna 0.2/1.2, terra 2/12) unchanged vs cache; plan data (Pro $100, message allowances) not re-verifiable publicly this session → carried | models.dev api.json live + 09-10 cache diff |
| crof | **GATE STAYS ON + PAYG STICKERS NOW PUBLIC** — crof.ai/pricing publishes per-model $/M (deepseek-v4.1-flash 0.10/0.50, kimi-k3 2/8, glm-5.2 0.30/1.05 …) matching models.dev exactly; wrapping-canary (R-10/A10) status not publicly verifiable → gated until measured | crof.ai/pricing; models.dev `crof` |
| gw-deepseek | no-change verified (archived gateway-alias; mirrors deepseek terms) | — |
| deepseek-foreman | no-change verified (dedicated PAYG key lane; deepseek terms apply) | api-docs.deepseek.com/quick_start/pricing |
| deepseek-duckbrain-sync | no-change verified (same PAYG terms, dedicated key) | same |
| myrouter:zai-glm | no-change verified (archived gateway-alias; zai terms above apply) | docs.z.ai (see zai-glm) |
| commandcode | no-change verified — no-markup sticker model intact (its rows price from models.dev; today's live fetch = zero drift on its covered families) | models.dev api.json |
| sambanova | no-change verified — disabled-by-config (dead key 401, documented 09-02) | (prior evidence stands; not re-keyed — no key available) |

## ToS / data-class review (providers.jsonl tos_class / data_class / trains_on_hosted)

- zai Team Plan doc: "Data is not used for model training by default" —
  consistent with `data_class: zdr`, `trains_on_hosted: false`. Individual
  plan terms unchanged on this point. **No change.**
- Kimi: batch/BYOK ban still in force (plan page + prior ToS capture) —
  `tos_class: interactive-only (batch BANNED)` **stands**.
- Meta: Contributor tier explicitly training-opt-in WITH discount; Standard
  not trained on — matches `muse-spark` contributor rows and meta-model
  `zdr`. **No change.**
- Ollama: "Prompt or response data is never logged or trained on" — zdr
  stands. **No change.**
- Synthetic: zero-retention stated on zen/privacy pages previously; pricing
  page unchanged on data terms. **No change.**
- grok-build remains `consumer-sub` + `training-eligible` (grok.com free/
  consumer terms unchanged publicly). **No change.**
- No provider visited this session changed its ToS class, data class, or
  training flag → **providers.jsonl untouched by this lane.**

## Quota-state gate triggers (docs/registry-maintenance.md §5)

- **grok-build** — pool size STILL unpublished (grok.com/plans 2026-09-11:
  shared Chat/Imagine/Voice/Build pool, no numbers). Gate stays;
  `window_exhausted false`, `concurrency_free false` still correct.
- **crof** — wrapping canary (R-10/A10) closure not publicly verifiable;
  crof.ai now publishes PAYG stickers (inflation mechanism measurable
  later), but no canary result. Gate stays.
- clinepass weekly-cap gate (09-05 429 INFERENCE_CAP_ERROR) is health-gate
  state, not plan terms — the caps themselves were re-confirmed via the
  live plans API; gate clears on the next green probe, not by edit.

## Temporary discounts sweep (2026-09-11)

- 19 open `:free` clinepass rows: **all alive** on GET /api/v1/models (under
  org-prefixed id forms). No NEW free lanes appeared (the 09-05/09-09/09-11
  additions are already tracked). No lane died beyond the 3 already marked
  REVOKED 09-09 (minimax-m2.7:free, minimax-m3:free, glm-5.2:free).
- `ling-3.0-flash-sante:free` remains tracked with the RESTORED 09-09 note.
- Rows updated with the ID-FORM note (org form recorded, bare id kept per
  registry convention). The new `ling-3.0-flash-vl:free` row (valid_from
  2026-09-11) predates this tick and was verified live.

## Seed-input changes made (all via the generated path)

| file | change |
|---|---|
| `data/tables/plan_terms.jsonl` | opencode-go: +`requests_per_5h` (27 Go-page rates), note/source rewritten; kimi-for-coding: plan_cost 19→15 + refreshed plan/refresh terms; synthetic: pack economics measured; clinepass: caps confirmed + discount flag closed; ollama-cloud: per-model rates published (gap closed) + credit-plan lineup note; zai-glm: multiplier-table revision (5.3/5.3-flash only; 4.7→flash routing) |
| `data/tables/temporary_discounts.jsonl` | 19 :free rows +ID-FORM note (org-prefixed live id recorded, lane aliveness verified 09-11) |
| `data/tables/models.jsonl` | clinepass: +7 paid non-included lanes (documented PAYG gaps, full schema); 15 :free rows re-enabled (id-form artifact, evidence updated); opencode-go: 26 lanes re-priced via researched rates (evidence `opencode-go: sub-bucket 12/req/31250 (rate=opencode.ai/go)`); deepseek: 2 lanes re-priced to today's OR spot |

Reprice ran `--dry-run` FIRST (30-row diff shown above), then the real run;
`reprice → seed → export → snapshot → commit` all green. Both namespace
repos committed on master (routing ec75ace, task-router 560d779); nothing
pushed (foreman pushes s3daily).

## Follow-ups surfaced (not lane-2 scope)

1. **ollama-cloud 402** — human billing check at ollama.com (pitfall doc
   09-11 rule: uniform 402 = billing event, escalate). If the grandfathered
   Max is gone, the 21 lanes' bucket-plan economics need a decision
   (credit bucket at sticker → lane prices still valid as sticker caps).
2. **zai-glm glm-4.7** — registry row 0.138 (old own-multiplier) vs new
   flash-rate 0.069: chain-neutral (price falls), but the row's evidence
   string should follow the next seed refresh; leaving to the daily slice
   keeps this lane review-only.
3. **grok-build pool** — unchanged; pilot-ledger measurement remains the
   only unlock.
4. **models.dev `openai-codex` provider entry gone** — worth a lane-1
   catalog check (registry rows unaffected today).
